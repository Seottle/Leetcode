
定义一个队列Q
定义一个备忘录memo，记录访问过的数据

判断直接返回条件

将初始数据放入队列中
标记初始数据已访问

while(!Q.empty()) { //遍历队列
    获取当前队列中的元素个数长度 size

    遍历当前队列for(size) {
        取出队头元数

        判断剔除不符合规则的元素
        1.越界, continue 2.已符合要求，直接return 3.在备忘录中有出现，continue

        push入队列中
        在备忘录中标记    
    }
}